import { TranslationsGroup } from "./translations-group.model";

describe('TranslationsGroup', () => {
  it('should create an instance', () => {
    expect(new TranslationsGroup('group1')).toBeTruthy();
  });
});
